# Port the HTTP server listens on (defaults to 3000 when unset)
PORT=3000

# Secret used for HMAC-SHA256 payload signatures. Required, no default.
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HMAC_SECRET=
